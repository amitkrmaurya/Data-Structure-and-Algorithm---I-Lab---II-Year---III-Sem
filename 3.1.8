#include <stdio.h>
#include <math.h>

int main() {
    int N;
    scanf("%d", &N);

    // Calculate 2^N - 1 using bitwise left shift
    int moves = (1 << N) - 1;

    printf("%d\n", moves);
    return 0;
}

